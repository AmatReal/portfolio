---
interface Tag {
  label: string;
  icon?: string;
}

interface Props {
  glyph: string;
  gradient: string;
  title: string;
  description: string;
  tags: Tag[];
  ariaLabel: string;
}

const { glyph, gradient, title, description, tags, ariaLabel } = Astro.props;
---

<article class="card">
  <button
    type="button"
    class="card-media"
    data-modal-open
    aria-haspopup="dialog"
    aria-controls="project-modal"
    aria-label={ariaLabel}
    style={`background:${gradient}`}
  >
    <span>{glyph} {title}</span>
    <span class="card-media-tag"><i class="uil uil-eye"></i> Click for more</span>
  </button>
  <div class="card-body">
    <h3><a href="#">{title}</a></h3>
    <p>{description}</p>
    <div class="tags">
      {tags.map((tag) => (
        <span class="tag">
          {tag.icon && <i class={`uil ${tag.icon}`}></i>}
          {tag.label}
        </span>
      ))}
    </div>
  </div>
</article>